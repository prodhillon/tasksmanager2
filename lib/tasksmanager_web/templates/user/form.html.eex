<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :email, class: "control-label" %>
    <%= email_input f, :email, placeholder: "user@example.com", class: "form-control" %>
    <%= if message = f.errors[:email] do %>
    <p class="text-danger">
      <span><%= translate_error(message) %></span>
    </p>
      <% end %>
  </div>

  <div class="form-group">
    <%= label f, :name, class: "control-label" %>
    <%= text_input f, :name, class: "form-control" %>
    <%= if message = f.errors[:name] do %>
    <p class="text-danger">
      <span><%= translate_error(message) %></span>
    </p>
      <% end %>
  </div>

  <div class="form-group">
    Are you a reporting manager in your firm? : <br/>
    <%= select f, :manager, ["No": "No", "Yes": "Yes"], class: "form-control" %>
    <%= if message = f.errors[:manager] do %>
    <p class="text-danger">
      <span><%= translate_error(message) %></span>
    </p>
      <% end %>
  </div>

  <div class="form-group">
    Please select your reporting manager if any :
    <%= select f, :select_manager_id, @allusers ,class: "form-control" , prompt: "-- None --" %>
    <%= if message = f.errors[:select_manager_id] do %>
    <p class="text-danger">
      <span><%= translate_error(message) %></span>
    </p>
      <% end %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
